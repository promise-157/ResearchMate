# 当前路线图

本文是新会话的当前状态入口；只记录已经验证的事实和下一个最小里程碑。

## 已完成：M1 通用文字资料闭环

验收条件：

- [x] 工作区数据库可幂等创建通用资料表，旧论文数据不丢失。
- [x] 用户可导入一段文字并选择/接受资料类型。
- [x] 系统规范化文本、生成内容指纹并阻止精确重复。
- [x] 用户可搜索、按类型/状态筛选和查看资料。
- [x] 用户可修改资料状态，不存在只停留在浏览器的假操作。
- [x] API、存储和前端流程有离线自动测试。
- [x] README 和使用手册与实际行为一致。

## 已完成基础

- Codex 仓库入口、安全边界和产品/架构文档。
- arXiv 公共 API 同步及默认关闭通用 HTML 抓取。
- API Key 非持久化、后端代理和显式论文 AI 附件。
- 工作区导入与路径保护，以及现有核心契约测试。

## 已完成：M2 可审计的通用资料 AI

验收条件：

- [x] 用户从资料详情主动选择分析类型并确认将发送的字段范围。
- [x] 浏览器只调用本机 API，外部模型调用只发生在后端；未配置模型时给出可行动错误。
- [x] 每次运行记录资料 ID、输入哈希、处理器/提示词版本、服务商、模型、状态、结果或错误。
- [x] AI 返回必须经过结构校验，只形成建议和运行记录，不覆盖来源事实或用户确认值。
- [x] 重复输入可识别已有运行，失败不会显示成功，刷新页面后仍可查看历史结果。
- [x] 自动测试使用假模型响应，不使用真实 Key、真实网络或付费调用。
- [x] 支持 2–20 条资料的比较/归纳，明确显示输入清单，每条正文最多发送 3,000 字符。

## 已完成：M3 用户图片与本地 OCR

验收条件：

- [x] 用户可导入 PNG、JPEG 或 WebP 图片，单文件不超过 10 MB。
- [x] 后端校验图片签名，以文件 SHA-256 去重，并将原件保存到工作区关联的本地资产目录。
- [x] 资料详情可安全读取和预览图片，浏览器不接触任意本地路径。
- [x] 用户可主动运行本地 Tesseract OCR；未安装时给出可行动错误，不回退到外部服务。
- [x] OCR 输入哈希、处理器版本、状态、文本结果或错误写入处理历史，刷新后仍可查看。
- [x] 清空或删除工作区时同时删除对应资产；离线测试只使用临时文件和假 OCR。

## M2/M3 集中验证与后续处理清单

M2、M3 的离线契约和构建已经通过；下列项目因安全策略、当前机器环境或里程碑边界尚未完成。以后统一处理时按编号更新状态，不应把它们误记为已经验证。

| ID | 状态 | 待处理项 | 当前证据与统一处理方式 |
| --- | --- | --- | --- |
| M2-V1 | 待授权烟测 | OpenAI、Claude、Ollama 和自定义兼容接口的真实响应格式与错误映射 | 自动测试只使用假模型，未读取真实 Key 或调用付费接口。由用户提供非敏感样例并明确授权后，逐个服务商验证成功、无 Key、超时、非 JSON 和 HTTP 错误。 |
| M2-V2 | 待端到端验收 | 浏览器中的单条分析、2/20 条比较、失败历史、刷新与工作区切换 | 当前证据是服务/API 单元测试、前端 lint 和生产构建，没有浏览器 E2E。统一补 Playwright 离线假后端测试或进行一轮手工验收。 |
| M3-V1 | 待真实样本验收 | PNG、JPEG、WebP 的真实解码与浏览器预览 | 自动测试覆盖大小、文件头、哈希、隔离路径和读取，但 fixture 不是完整可解码图片。使用无敏感信息的三种真实小图补 fixture/浏览器验收。 |
| M3-V2 | 环境阻塞 | 本地 Tesseract 的真实 OCR、中文语言包和错误输出 | 当前机器未安装 `tesseract`；自动测试只使用成功/失败假处理器。安装 Tesseract 与所需语言包后，用无敏感图片验证英文、中文、空白图和超时。 |
| M3-V3 | 待回归测试 | 清空/删除工作区后资产目录确实删除，其他工作区资产不受影响 | 删除逻辑已经实现，但没有专项自动测试。补两个临时工作区的隔离清理测试。 |
| M3-L1 | 已知限制 | 工作区 `.db` 导出/导入不携带图片原件 | 当前导出仅返回 SQLite 文件；跨机器导入后图片资产会缺失。后续需要定义包含 DB 与资产清单/哈希的安全归档格式，在完成前不要把 DB 导出视为图片工作区完整备份。 |
| M3-L2 | 已完成（M8） | OCR 文本进入可选搜索和显式 AI 输入 | 成功 OCR 可预览并显式接受到 `accepted_extractions`；不覆盖原始正文。搜索默认不扩大，单条/多条 AI 也只有明确勾选 `accepted_extraction` 才读取接受值。 |
| M3-S1 | 待加固 | 图片只校验文件签名，未用完整图片解码器验证结构 | 当前能拒绝伪造扩展名，但截断或畸形图片可能入库后无法预览/OCR。引入本地解码器时需限制像素总量并防止解压炸弹，不能仅按扩展名判断。 |

## 已完成：M4 单个公开 URL 导入和候选箱

验收条件：

- [x] 用户可从资料中心明确提交一个公开 HTTP/HTTPS URL，普通文字资料的来源字段仍只记录 provenance。
- [x] 具名 `single_public_url` 采集器拒绝认证 URL、非标准端口和本机/私有/保留地址；逐跳校验 DNS、连接对端和 robots 策略，并禁用环境代理。
- [x] 单次读取限制 3 次重定向、30 秒总耗时、1 MB 解压后 HTML 和静态 HTML 内容类型，不登录、不执行脚本或下载附件。
- [x] 最小标题/正文、最终 URL、采集器、采集时间和 HTTP 元信息先保存到独立 `candidates`，不会自动写入 `items`。
- [x] 候选箱展示待审核结果与失败任务；接受、拒绝及重复接受均持久化，接受时复用通用规范化、分类和指纹去重。
- [x] API、service、storage、失败映射和 HTML/robots/SSRF 边界使用内存 HTTP fixture 测试；前端 lint 通过且没有真实网络采集。

## 已完成：M5 论文映射到通用资料核心

验收条件：

- [x] 旧工作区通过幂等迁移为每条兼容 `papers` 记录保存 `item_id`，原论文记录和已有通用资料不丢失。
- [x] 映射后的论文可在资料中心以通用资料搜索、筛选和参与显式 AI，同时保留论文中心、购物车和专用字段。
- [x] 指纹命中已有资料时只建立映射，不覆盖用户标题、类型、标签、状态或 metadata。
- [x] 新 arXiv 记录在论文写入事务内同步创建映射，工作区论文数和资料数一并更新。
- [x] 论文来源 URL、兼容记录 ID、arXiv ID 和来源事实保留 provenance；默认不下载 PDF。
- [x] 旧库迁移、重复迁移、已有资料复用、新论文事务映射和论文 API 专用视图均有离线测试。

## M4/M5 验证与后续处理清单

| ID | 状态 | 待处理项 | 当前证据与统一处理方式 |
| --- | --- | --- | --- |
| M4-V1 | 待真实来源验收 | 公网 DNS、TLS、robots、重定向和常见字符集 | 自动测试只使用内存 HTTP fixture，未访问真实网站。由用户明确选择允许测试的无敏感公开页面后，逐项验证允许、robots 拒绝、重定向、超时、超限和非 HTML。 |
| M4-V2 | 待端到端验收 | 浏览器提交、失败任务、接受/拒绝、刷新和工作区切换 | 当前证据是 API/service/storage 测试、前端 lint/构建；补 Playwright 离线假后端测试或一轮手工浏览器验收。 |
| M4-L1 | 已知限制 | 静态 HTML 启发式正文提取 | 不执行 JavaScript，也不做站点专用阅读模式；复杂页面可能提取导航噪声或缺少正文，应改用用户浏览后的文字/截图导入。 |
| M4-S1 | 待加固 | DNS 校验与连接仍依赖底层客户端时序 | 当前逐跳校验全部解析地址并在连接后复核对端、禁用代理，已覆盖常见 SSRF；后续可评估支持 TLS SNI 的地址固定 transport，进一步缩小 DNS 重绑定时序窗口。 |
| M5-V1 | 待端到端验收 | 真实旧工作区升级、论文中心与资料中心交叉查看 | 临时 SQLite 迁移和 API 测试已通过，尚未在用户真实数据副本及浏览器中验收。先备份，再用副本验证计数、筛选、购物车和 M2 分析。 |
| M5-L1 | 已知限制 | 来源刷新不自动重写已映射资料正文 | 为避免覆盖用户修改，“全部重新同步”只更新 `papers` 来源事实。后续需要显式预览差异并接受的可审计重新处理动作。 |
| M5-L2 | 兼容边界 | 旧论文 AI 字段尚未迁移到通用审计运行 | 旧字段继续供论文专用视图使用；新的分析应从映射后的通用资料发起。后续决定只读展示或一次性 provenance 迁移。 |

## 已完成：M6 Debug 模板与本地近似检索

本里程碑按“先选择一个最小真实第二用例”的约束选择 Debug；求职模板不被误记为已完成。

验收条件：

- [x] Debug schema v1 定义错误、环境、尝试、根因和最终方案，存储层分别保存确定性提取值与用户确认值。
- [x] 用户打开 Debug 资料时可运行本地标签规则提取；运行记录包含输入哈希、处理器版本、范围、状态和结果，不调用 AI。
- [x] 用户可编辑并持久保存确认值；重新提取只更新提取层，不覆盖确认层。
- [x] 资料中心可按 Debug 错误字段筛选，并在详情中清楚展示提取值与确认值来源。
- [x] 用户可对任意资料运行本地 `token-jaccard-v1` 近似检索，查看分数和共同 token；关系及证据写入 `item_relations`。
- [x] UI、API、service、storage、错误状态和离线自动测试形成完整纵向切片；不读取 Key、不调用付费 AI 或真实网络。

## 已完成：M7 arXiv 公开 API 发现与持久任务

本里程碑完成第一个具名多结果发现适配器；RSS 和其他公开来源仍需各自形成真实用例后再加入。

验收条件：

- [x] 用户从资料中心明确输入 arXiv 搜索词和 1–20 条上限，浏览器只调用本机 `/api`。
- [x] 后端 `arxiv_api` 适配器只请求 arXiv 官方公开 API，限制 25 秒、2 MB 和单次结果数，不下载 PDF、不读取凭证。
- [x] 每次查询先创建持久 `collection_jobs`，保存适配器、查询、状态、候选数、接受数和可见错误。
- [x] M4 的一任务一候选 schema 幂等迁移为一任务多候选，旧候选不丢失；重复迁移安全。
- [x] 搜索结果保留 arXiv ID、作者、分类、发布时间、来源 API 和 URL provenance，只进入候选箱。
- [x] 候选逐条接受或拒绝；接受时复用内容指纹去重并创建论文类型通用资料，不写旧论文兼容表、不下载 PDF。
- [x] API、service、storage、失败映射、Atom 解析和旧 schema 迁移使用离线 fixture 测试；前端有真实入口、候选审核和失败显示。

## M6/M7 验证与后续处理清单

| ID | 状态 | 待处理项 | 当前证据与统一处理方式 |
| --- | --- | --- | --- |
| M6-V1 | 待端到端验收 | 浏览器中的 Debug 提取、确认、重新提取、错误筛选、相似结果与工作区切换 | 当前证据是 service/API/storage 测试、前端 lint 和构建；补 Playwright 离线假后端测试或一轮手工浏览器验收。 |
| M6-L1 | 里程碑边界 | 求职模板与共享模板抽象 | 当前只完成首个 Debug 真实用例。出现求职第二用例后再提取共享注册/字段渲染抽象，避免提前设计。 |
| M6-L2 | 已知限制 | 同义表达、跨语言和短文本的语义相似 | 当前算法只比较本地词元集合并展示证据；后续评估本地 embedding。任何外部 embedding 都必须明确展示和确认资料范围。 |
| M6-L3 | 已知限制 | Debug 标签规则只识别按行的中英文标签 | 自由叙述可能提取为空，用户仍可填写确认值。后续用脱敏离线 fixture 扩展规则，不能制造推断事实。 |
| M7-V1 | 待真实来源验收 | arXiv 官方 API 的真实 Atom、HTTP 错误、超时和响应边界 | 自动测试只使用本地 Atom/失败 fixture，未访问真实网络。由用户明确授权后用无敏感搜索词验证。 |
| M7-V2 | 待端到端验收 | 浏览器搜索、多候选接受/拒绝、失败、刷新和工作区切换 | 当前证据是纵向单元测试、前端 lint 和构建；补 Playwright 离线假后端测试或一轮手工浏览器验收。 |
| M7-L1 | 里程碑边界 | RSS 和其他公开 API/搜索来源 | 当前只有具名 `arxiv_api`。新来源必须有明确用户价值、来源契约、数量/超时/频率限制、provenance 和离线 fixture。 |
| M7-L2 | 已知限制 | 进程中断后的 `running` 任务恢复 | 正常成功和失败会落盘，但进程在请求中被强制终止可能留下 `running`。后续增加启动时中断标记或可恢复后台执行器，不能把残留任务显示为成功。 |

## 已完成：M8 离线端到端与提取结果接受

验收条件：

- [x] 工作区通过幂等迁移新增 `accepted_extractions`，保存资料、提取类型、运行引用、当前文本和接受时间；现有资料与运行历史不丢失。
- [x] 成功的本地 OCR 在详情中显示文本预览，只有用户明确点击接受才写入接受层；空结果、失败运行、非受支持运行和跨资料运行会拒绝。
- [x] 接受或重新接受不改写 `items.content_text`，同一资料同一提取类型只更新当前采用版本，历史运行继续保留。
- [x] 搜索默认仍只查标题和原始正文；只有勾选“同时搜索已接受提取文本”时才扩展到接受层。
- [x] 单条 AI 和 2–20 条比较可独立选择 `accepted_extraction`；审计范围记录该字段，单条与比较分别限制 12,000/3,000 字符，未选择时不会读取或发送。
- [x] UI、API、service、storage、迁移、失败状态和离线自动测试一致，不读取 Key、不调用真实 AI/Tesseract 或网络来源。
- [x] Playwright 离线假后端 E2E 已实际通过，覆盖 OCR 预览/接受、刷新、显式搜索、切换到隔离工作区、在该工作区刷新及切回后恢复接受值。
- [x] 产品、架构、快速上手、使用手册、README 和变更记录已同步实际行为。

完成验证（2026-08-07）：

- `PYTHONDONTWRITEBYTECODE=1 conda run -n researchmate python -m unittest discover -s tests -v`：46 项通过。
- `PYTHONDONTWRITEBYTECODE=1 conda run -n researchmate python -m compileall -q src/backend`：通过。
- `conda run -n researchmate ruff check src/backend tests`：通过。
- `cd src/frontend && npm run lint && npm run build`：通过；保留大 chunk 警告见 M8-L3。
- `cd src/frontend && npm run test:e2e`：Chromium 1 项通过，使用离线 API fixture。
- `git diff --check`：通过。

## M8 验证与后续处理清单

| ID | 状态 | 待处理项 | 当前证据与统一处理方式 |
| --- | --- | --- | --- |
| M8-V1 | 待真实环境验收 | 完整可解码图片 + 本机 Tesseract 的浏览器 OCR、预览、接受、搜索和 AI 范围显示 | service/API 使用假 OCR，Playwright 使用离线假后端，均未调用真实 Tesseract。安装 Tesseract 与所需语言包后，用无敏感 PNG/JPEG/WebP 验证；外部 AI 仍不得在该验收中静默调用。 |
| M8-V2 | 待扩展端到端验收 | M2、M4、M6、M7 的剩余浏览器流程 | 本次 E2E 只完成 M8 的关键刷新/工作区隔离路径，不代表 M2-V2、M4-V2、M6-V1 或 M7-V2 已完成；仍需各自补离线浏览器 fixture 或手工验收。 |
| M8-L1 | 里程碑边界 | 通用接受层目前只接受本地 OCR 文本 | Debug 继续使用自身的提取/用户确认分层。第二个确需通用接受的确定性处理器出现后，再扩展受支持类型和结构化值，不接受 AI 建议冒充确定性事实。 |
| M8-L2 | 已知限制 | 接受文本使用 SQLite `LIKE` 扫描 | 当前满足工作区内显式搜索，尚无 FTS 索引、命中片段或排序。资料量和真实查询证明需要后再设计可迁移的本地索引。 |
| M8-L3 | 已知性能债务 | 前端生产包存在超过 500 kB 的 chunk 警告 | `npm run build` 成功，但主 chunk 约 1.10 MB（gzip 约 365 kB）。后续用路由/组件拆分和手动 chunk 验证首屏收益；当前不把构建警告误记为失败，也不在 M8 内做无关重构。 |

## 下一阶段：M9 求职模板第二真实用例

下一阶段以求职资料为第二个模板真实用例，先定义最小字段、来源层次和用户确认流程，再从 Debug 与求职的共同代码中提取共享模板注册/渲染抽象。不得提前把 M6-L1 标记完成。RSS 或其他来源也必须逐个完成具名纵向切片。

## M9 之后的建议顺序

以下是当前遗留问题推导出的建议顺序，不是已完成事项，也不是跳过每阶段验收的长期承诺；每完成一个里程碑后都应根据真实使用结果重新核对。

1. **M10 跨里程碑浏览器回归**：偿还 M2-V2、M4-V2、M6-V1、M7-V2，分别覆盖成功、失败、刷新和工作区切换。离线假后端 E2E 与真实环境验收继续分开记录。
2. **M11 图片与工作区数据完整性**：完成图片解码/像素与解压炸弹防护、隔离清理回归，以及包含数据库、资产清单和哈希的可移植归档，解决 M3-V1、M3-V3、M3-L1、M3-S1。
3. **M12 显式重新处理与任务恢复**：为来源刷新增加差异预览/接受，处理进程中断后残留 `running` 任务，优先偿还 M5-L1 和 M7-L2；不得用自动覆盖或假成功简化。
4. **M13 本地检索扩展**：先用真实数据验证 SQLite FTS、命中片段和排序需求，再评估本地 embedding。外部 embedding 仍须明确展示和确认范围。
5. **后续具名来源**：RSS 或其他公开 API 只有在出现明确用户价值后才立项，每个来源单独完成限制、provenance、失败状态和离线 fixture，不恢复泛化抓取。

真实 Tesseract、真实模型服务商、真实网页/arXiv 网络行为始终作为需环境或授权的独立验收项。工程整体完成后再卸载 WSL 用户缓存中的 Playwright Chromium；该缓存不在 Git 仓库内，也不会上传 GitHub。
